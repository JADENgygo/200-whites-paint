# AGENTS.md

このファイルはリポジトリ全体に適用されます。サブディレクトリに別の `AGENTS.md` がある場合は、より近い階層の指示を優先してください。

## プロジェクト概要

- Node.js 24 と TypeScript を前提としたプロジェクトです。
- 現在は初期構築段階です。実装に先立ち、`package.json`、README、設定ファイル、既存コードを確認し、実際に採用されている構成に従ってください。
- フレームワーク、パッケージマネージャー、ディレクトリ構成を根拠なく決め打ちしないでください。

## 作業方針

- 依頼された範囲に絞り、必要最小限で一貫した変更を行ってください。
- 既存の設計、命名、コードスタイルを優先してください。
- 無関係な変更やユーザーの未コミット変更を上書きしないでください。
- 新しい依存関係は、標準機能や既存依存で代替できない場合に限って追加してください。追加理由を作業結果に記載してください。
- API キーやトークンなどの秘密情報をコミットしないでください。環境変数の例が必要な場合は `.env.example` を使用してください。

## 実装規約

- TypeScript では型安全性を重視し、理由のない `any`、型アサーション、型チェックの無効化を避けてください。
- 関数やモジュールは責務を小さく保ち、意図が伝わる名前を付けてください。
- エラーを握りつぶさず、利用者が対処できる情報を含めて扱ってください。
- コメントは処理内容の言い換えではなく、判断理由や制約の説明に使ってください。
- 公開される挙動を変更する場合は、関連するドキュメントとテストも更新してください。

## コマンドと依存関係

- パッケージマネージャーは既存のロックファイルから判断してください。ロックファイルがなければ `package.json` の `packageManager` を確認してください。
- 検証には `package.json` の scripts を使用し、存在しないコマンドを想定しないでください。
- 依存関係を変更した場合は、対応するロックファイルも更新してください。複数種類のロックファイルを作成しないでください。
- 生成物、キャッシュ、`node_modules`、秘密情報をコミットしないでください。

## テストと検証

- 不具合修正には、可能な限り再発を防ぐテストを追加してください。
- 新機能には正常系だけでなく、主要な境界値と失敗系のテストも含めてください。
- 変更後は、利用可能な範囲でテスト、型チェック、lint、ビルドを実行してください。
- 実行できなかった検証がある場合は、そのコマンドと理由を最終報告に明記してください。

## 完了時の報告

- 変更内容を簡潔に要約してください。
- 実行した検証と結果を記載してください。
- 残課題、互換性への影響、利用者側の追加作業がある場合は明示してください。
- 最後にプルリクを出してください

## プルリクエストの作り方

- devブランチを基準にfeature/xxxブランチを作成
- xxxは実装した機能の名前
- 作成したfeature/xxxブランチからdevブランチへのマージ
- 日本語が使える箇所は日本語を使う
