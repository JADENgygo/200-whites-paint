# 作成するもの

ペイントウェブアプリ

## 環境について

- 言語はtypescript
- viteでビルド
- biomeで整形、静的解析を実施
- vitestで単体テストを実装
- 開発時はviteの開発サーバーを8080で建てて公開

## 仕様

### パレット

- 使える色は白200色 (それぞれ微妙に異なる色)
- 並べ方は左上が一番明るく、右下が一番暗い
- 画面上部にパレットががある、サイズは可能な限り小さく、ただし操作困難になってはいけない
- パレット部分は実際の色を表示して選択できるようにする、カーソルを重ねると実際の色の名前とRGBとHSVを表示
- 色名とRGB値は実在する色名データの同じ組み合わせを使用する
- 日本語名がある色は `英語名 (日本語名)` の形式で表示し、英語名と括弧の間には半角スペースを入れる
- パレット表示を最小化できるようにする
- 初期色は筆が一番明るく、背景が一番くらい

### 描画画面

- 下部に描画画面
- 全部の画面がFHDの大きさに収める (スクロールを発生させない)
- 描画画面の背景色もパレットから選べるようにする

### OGP

- OGPとTwitter Cardを設定し、タイトル、説明、画像を表示できるようにする
- 公開URLとcanonical URLは`https://200-whites-paint.pages.dev/`とする
- OGP画像は1200×630pxの`/ogp.png`とし、公開URLを含む絶対URLで指定する

### ダークモード

- ダークモードに切り替えられるようにする (切替後は保存、初期表示はos設定に依存、判断不能の場合はライトモード)
- ダークモードの色は真っ黒ではなく少し明るい黒

### その他

- ヘッダーはタイトルの200 WHITESと右端にx(@JADENgygo)のアイコンリンク
- フッターは不要
- pngで画像を保存するボタンとクリップボードにコピーするボタンを描画画面下に配置。保存ファイル名は`200-whites.png`とする
- 「キャンバスを消す」を元に戻すボタンを作成
